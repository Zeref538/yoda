"""YODA — Your Offline Data Agent.

Privacy-first, fully local data-cleaning agent. The LLM only ever sees
metadata (schema + statistical profile + redacted samples); deterministic
pandas code executes every change with a full audit trail.
"""

__version__ = "0.2.0"
